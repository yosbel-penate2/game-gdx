./gradlew clean build                    # Build everything, Remove build folders

./gradlew test                     # Run unit tests
./gradlew lwjgl3:run              # Build and run the game
./gradlew lwjgl3:jar              # Build runnable JAR (in lwjgl3/build/libs)             # Build everything   